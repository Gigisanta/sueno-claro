# Hermes prompt — implement iOS + RevenueCat

Use this only after web MVP shows traction or Gio explicitly asks to build iOS.

Mandatory:

- Load `coding-preflight`, `repo-edit`, `source-driven-development`.
- Read RevenueCat official iOS docs and current SDK docs.
- Read `docs/06-monetization-revenuecat.md`, `plans/phase-2-ios-revenuecat.md`.
- Do not request microphone/HealthKit permissions.
- Implement local calculator first, then RevenueCat.
- Verify sandbox purchases and restore before reporting success.

Blockers to ask for only when needed:

- Apple Developer account access.
- RevenueCat project public SDK key.
- App Store Connect product IDs.
